---
name: ling-fun-illustrations
description: 为中文文章、博客、公众号、帖子、视频脚本、口播稿、SRT 和录屏内容设计并生成固定卡通人物的 16:9 趣味配图。用于文章正文插图、视频 B-roll 补画、时间轴补图、连续镜头、配图策略、shot list、图像生成和局部改图；把观点、情绪、流程、因果、对比、场景与抽象概念转成 `#F5F5F7` 浅灰底、主体居中、信息清楚且带冷幽默的手绘画面，并严格引用 `assets/character/` 中的身份与配色锚点。
---

# Ling趣味配图

## 核心定位

为中文文章和视频内容设计并生成 16:9 横版趣味配图。既可以为文章、博客、公众号、帖子和 Notion 文档制作正文插图，也可以根据视频脚本、口播稿、SRT 或录屏缺口制作 B-roll 配图与连续镜头。把观点、情绪、场景、因果、流程、结构、状态或隐喻，变成简单、干净、信息清楚并带有轻松笑点的手绘画面。

本技能里的固定主角统一叫“Ling”。Ling 是长期核心 IP，不是临时生成的匿名卡通人物：成年女性漫画脸，柔和圆润的下颌，夸张的大头小身体，暖肤色，黑色长直发（中分、垂至肩下），细眉，银色细框圆眼镜，浅紫色短袖 T 恤，细黑线胳膊和腿；常用温柔注视或侧眼形成专注的喜剧感。默认每张图都由 Ling 承担最重要的动作；不要用其他人物替代 Ling，不要无缘无故生成多个同等主角，也不要在同一组图中改变 Ling 的脸型、五官、发型、眼镜、头身比例和默认服装。

Ling 必须亲自操作、验证或承受画面的知识关系，用一本正经的反应制造冷幽默，不能只站在旁边当装饰。知识信息永远优先于笑点；笑点只负责降低理解压力，不能歪曲事实。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `references/style-dna.md`：每次做 shot list 或生图前必读；它是风格、颜色和“文字配额”的单一权威。
- `references/ling-ip.md`：Ling IP 的形象、性格、表情、动作库和禁忌。
- `references/composition-patterns.md`：结构类型、视频连续帧、原创隐喻方法和反复刻规则。
- `references/prompt-template.md`：每次写单张生图提示词时读取并完整填写。
- `references/qa-checklist.md`：每张生成图完成后按层验收。
- `assets/character/ling-fullbody-original.png`：Ling 身份主锚点。
- `assets/character/ling-expression-sheet-original.png`：Ling 表情与动作主锚点。
- `assets/character/ling-expression-sheet-f5f5f7.png`：最终配色、明暗和整体感觉的权威参考，保持偏白、轻盈、平整且有少量自然层次。
- `assets/character/ling-fullbody-f5f5f7.png`：单人物处理版，用于辅助校准近景五官、发型、眼镜和服装细节。
- 如处理版与原图的角色结构出现细微差异，以两张 `*-original.png` 的身份特征为准；颜色与整体明暗以多人物处理版为准。
- `assets/examples/`：仅作次级、低频的构图与喜剧节奏研究。不得从这些图片借用角色造型；角色身份永远以 `assets/character/` 中的四张资产为准。

把固定引用顺序称为“三锚锁”：每次 `image_gen` 都按顺序传入 `ling-fullbody-original.png`、`ling-expression-sheet-original.png`、`ling-expression-sheet-f5f5f7.png`。连续帧把上一帧追加为第 4 张；只有近景需要更多五官、发型、眼镜或服装细节时，才把 `ling-fullbody-f5f5f7.png` 追加为第 5 张。

## 工作流

### 1. 消化正文

先读用户给的文章、帖子、博客、视频脚本、口播稿、SRT、链接、Notion 页面、Markdown 文件、录屏说明或截图内容。提炼：

- 核心观点是什么
- 哪些段落承担认知转折
- 哪些内容适合用图解释、承接情绪、补充 B-roll 或完成转场
- 哪些地方只适合文字，不需要图

不要平均配图。优先选择“认知锚点”，例如：关键因果、反直觉事实、两个断点、输入输出闭环、分流、前后对比、尺度差异、常见误区、角色状态变化。

如果用户同时提供“全文”和“要配图部分”，用全文理解上下文，只在指定部分生产配图。操作录屏已经清楚承载的步骤默认不重复画；优先为缺少画面、需要补充信息、承接语气、建立场景或完成转场的片段制作 B-roll 配图。保留 SRT 时间码，后续用于命名和剪辑定位。

如果用户还提供“已生成图片 / 配图时间表”和“要补图部分”，先做覆盖核对：把已采用图片视为固定成果，只补尚未覆盖的时间段和意义单元；不要重复表达、覆盖、改名或擅自重做用户已满意的图片。新图要承接相邻旧图的 Ling 外形、道具、镜头方向、强调色和叙事状态，并在时间表中明确区分“已有图”和“新增补图”。

完成标准：已明确核心观点、全部认知锚点、需要配图的范围，以及已有内容和待补内容的边界。

### 2. 先出配图策略

如果用户只是说“分析怎么配图 / 思考哪些地方需要配图”，先给 shot list。每张图写清楚：

- 放在哪个段落后
- 图的主题
- 核心意思
- 结构类型
- Ling 在图里做什么
- Ling 此刻的叙事情绪，以及眉眼、嘴形、身体姿态和步伐如何表达
- Ling 当前的视线目标、眼神方向，以及与前两张图相比如何避免三连重复
- 与前一张或后一张是否存在辛苦/轻松、困惑/理解、失败/成功等情绪反差
- 建议元素
- 画面文字方案：逐字写定核心句、辅助短句和物件标签，并统计预计字符数
- 笑点落在哪里
- 是否自然露出 `ling` 品牌名
- 视频任务的时间码与连续帧组

文章任务默认 4-8 张；文案很短时 1-3 张，长文也不要轻易超过 9 张。

视频口播稿或 SRT 不使用 9 张上限，按讲解节奏决定密度：

- 普通密度：每 2-4 句或每个完整意义单元对应 1 张图。
- 高密度补图：每 1-2 句，或约每两个逗号分隔的信息步骤，对应 1 个画面。
- 连续机制、操作链路或状态变化：生成 2-4 张连续帧；固定 Ling 身份、四肢骨架、主要物件、视角、线条和色彩，每张只推进一个动作；视线跟随当前注意对象，连续三张不得复用同一眼神方向和同一套眉眼嘴组合。
- 不为凑数量重复同一信息；也不要让一张图承担多个独立知识点。

完成标准：每张候选图都能对应一个明确意义单元，并写清结构、Ling 动作、叙事情绪、视线目标、与前两张的表情差异、连续关系、符合 `style-dna.md`“文字配额”的逐字画面文字、笑点和时间码；候选之间没有重复覆盖。

### 3. 单张生成

如果用户明确要求“生成 / 输出 / 做图 / 帮我生成”，不要停下来等确认；用内置 `image_gen` 每张单独生成并执行三锚锁。不要把多张图拼在一张里，也不能只在系列首张使用角色参考。

每张图只讲一个核心结构。提示词必须包含：

- 16:9 横版中文知识讲解正文配图
- 整张画布使用统一的 `#F5F5F7` 背景底色（RGB 245, 245, 247）
- 黑色手绘线稿
- 少量芥末黄/橙色/青蓝色/红色强调
- 大量留白
- Ling 作为核心动作主体
- Ling 是画面中唯一明确的核心主角，严格匹配两张原始角色资产，并与同组其他图片保持一致
- Ling 的两条胳膊和两条腿严格匹配角色资产：黑色单线四肢、黑点手、黑椭圆脚；动作只能改变单线方向，不生成紫色袖管手臂、肤色手掌手指、双线肢体、裤腿或鞋子
- 生成当前图前记录前两张图的视线方向、眼皮、眉毛、嘴形和头部角度；当前图连续三张不得使用同一眼神方向和同一套眉眼嘴组合，且变化必须跟随当前叙事与注意对象
- 主体内容整体居中，关键元素放在画面中间约 80% 的安全区域，左右各留约 8%-12% 白边，四周不贴边、不裁切
- 在系列首张、关键转场、Ling 重新出现或结尾等合适位置，自然加入拼写准确的小号手写品牌名 `ling`；不要每张都写，不要做成水印或大标题
- 先判断当前句在整段叙事中的情绪功能，再指定 Ling 的眉毛、眼睛、嘴形、躯干和动作；“冷幽默”不等于面无表情，表情方向必须与内容一致
- 遇到“但是、相反、终于、于是、只需要、轻松、免费”等转折或结果词，检查前后画面的情绪是否需要明显反转；至少用眉眼、嘴形、姿态或步伐中的两项让观众一眼看懂变化
- 先准确表达知识关系，再加入一个克制的视觉笑点
- 每张图必须出现能传递核心知识的中文手写文字，纯图直接判定失败；生图前按 `style-dna.md` 的“文字配额”逐字写定全部画面文字
- 禁止 PPT、商业插画、表情包、幼稚可爱、复杂架构、左上角类型标题

不要复刻参考视频或过往案例。案例只提供线条、留白、角色参与方式、表情力度和喜剧节奏，不能直接复用“篝火大笑 / 人肉闹钟 / 替罪挨打 / 水蛭收集 / 趴电脑看时钟 / Just Start for 5 Minutes”等已有场景。每次都要从当前文案重新发明一个简单、准确、略荒唐但成立的视觉隐喻。

完成标准：每张图独立生成、只表达一个知识关系；每次调用都执行三锚锁，Ling 的身份、配色和四肢骨架与资产一致，当前图与前两张不存在表情三连重复；画面不是纯图，实际出现的文字清楚、正确并符合文字配额。

### 4. 检查与迭代

生成后按顺序检查 `references/qa-checklist.md`：先过“四肢骨架闸门”，再过“表情三连闸门”，接着过“信息文字闸门”，最后检查身份、配色、知识和构图。前一层失败时先修正并重新验收，不要跳到后一层。

如果出现以下问题，优先重生成或局部编辑：

- Ling 只是装饰
- 主角不是 Ling、出现无必要的重复 Ling，或同组图片中的 Ling 外形明显漂移
- Ling 与角色资产不一致：柔和圆润脸、黑色长直发（中分）、细眉、温柔眼神、银色细框圆眼镜、浅紫色 T 恤或大头小身体比例发生漂移
- Ling 的任意可见四肢不是黑色单线和黑色圆点/椭圆端点，或出现紫色袖管手臂、肤色手掌手指、双线肢体、裤腿和鞋子
- 当前图与前两张图使用同一眼神方向和同一套眉眼嘴组合，形成连续三张表情重复
- 画面是纯图，文字没有传递核心信息，文字明显少于或多于文字配额，或者出现错字、乱码和难以辨认的字
- 需要品牌锚点时没有出现可读的 `ling`，或品牌名拼错、过大、像水印
- 主体偏向一侧、贴近边缘、被裁切，或左右没有安全留白
- 视频配图密度明显不足，长段讲解没有画面承接
- 连续帧中的 Ling、道具、视角或状态无故跳变
- Ling 的表情、姿态或步伐与当前语义相反，或转折前后仍是同一种情绪
- 补图与已有图片覆盖同一段内容、破坏原连续关系，或擅自替换用户已满意的图片
- 笑点抢走或歪曲知识信息
- 画面太满
- 太像流程图/PPT
- 中文堆成大段课件，或只剩几个无信息量的物件名称
- 左上角出现“常见误区/流程图/系统架构图”等类型标题
- 画风变成幼儿式可爱、表情包化、死板或过度精致，失去“温柔、聪明、专注”的成年漫画感
- 背景没有统一使用 `#F5F5F7`（RGB 245, 245, 247）

完成标准：每张最终图逐项通过 `references/qa-checklist.md`，发现的每个失败信号都已重生成或局部修正。

### 5. 保存交付

如果用户在 workspace 内工作，把最终图复制到：

```text
assets/<content-slug>-illustrations/
```

按顺序命名：

```text
01-topic-name.png
02-topic-name.png
```

保留原始生成文件，不要覆盖已有资产，除非用户明确要求替换。

如果输入包含 SRT 或明确时间码，同时交付一份配图时间表，至少记录：时间码、图片文件名、画面用途、是否属于连续帧组。补图任务还要标明已有覆盖与新增覆盖，避免重复使用同一时间段。

完成标准：全部最终文件和时间表已保存到约定路径，命名有序且没有覆盖用户已有资产。

## 输出口径

生成前的策略输出要短而准。生成后的交付要包含：

- 生成了几张
- 每张图的用途
- 保存路径
- 哪些图最稳，哪些图是可选
- 视频任务的时间码、连续帧组和可删减帧建议

不要长篇解释风格理论；让图自己说话。
